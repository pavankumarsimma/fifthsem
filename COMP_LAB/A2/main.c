#include "myl.h"
#include <stdio.h>

// main function
int main(){
    int n=0;
    printf("Enter : ");
    scanf("%d", &n);
    printf("%d", n);
	// printStr("Enter the integer value : ");
    // int m=0;
    // n=readInt(&m);
    // printInt(m);   
    // printInt(n);
    // printStr("Enter the float value : ");
    // float f=0;
    // n=readFlt(&f);
    // printFlt(f);
    // printInt(n);
	return 0;
}